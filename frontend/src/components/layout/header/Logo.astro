---
// Logo.astro
---

<a href="/" class="flex flex-col items-center logo-container no-underline">
  <div class="text-2xl font-bold tracking-wider uppercase logo-text">
    <span class="logo-char" style="--i: 1;">T</span>
    <span class="logo-char" style="--i: 2;">r</span>
    <span class="logo-char" style="--i: 3;">i</span>
    <span class="logo-char" style="--i: 4;">s</span>
    <span class="logo-char" style="--i: 5;">o</span>
    <span class="logo-char" style="--i: 6;">f</span>
    <span class="logo-char" style="--i: 7;">t</span>
  </div>
  <div class="text-xs tracking-widest text-gray-400 tagline">
    Ваш партнер 1С
  </div>
</a>

<style>
  .logo-container {
    display: inline-flex; /* Use inline-flex for better alignment */
    white-space: nowrap;
    color: #fff;
    transition: all 0.3s ease;
    text-decoration: none; /* Remove default link underline */
  }

  .logo-container:hover {
    color: #fff; /* Prevent text color change on hover */
  }

  .logo-text {
    display: inline-block;
  }

  .logo-char {
    display: inline-block;
    animation: flicker 3s infinite;
    animation-delay: calc(var(--i) * 0.1s);
    background: linear-gradient(45deg, #06B6D4, #8B5CF6, #D946EF);
    -webkit-background-clip: text;
    -webkit-text-fill-color: transparent;
  }

  .logo-container:hover .logo-char {
    animation: bounce 0.5s ease infinite alternate;
    animation-delay: calc(var(--i) * 0.05s);
  }

  .tagline {
    opacity: 0;
    transform: translateY(-10px);
    transition: opacity 0.5s ease, transform 0.5s ease;
  }

  .logo-container:hover .tagline {
    opacity: 1;
    transform: translateY(0);
  }

  @keyframes flicker {
    0%, 100% {
      opacity: 1;
      text-shadow: 0 0 5px #06B6D4, 0 0 10px #06B6D4, 0 0 20px #8B5CF6, 0 0 40px #8B5CF6, 0 0 80px #D946EF;
    }
    50% {
      opacity: 0.8;
      text-shadow: 0 0 2px #06B6D4, 0 0 5px #06B6D4, 0 0 10px #8B5CF6;
    }
  }

  @keyframes bounce {
    to {
      transform: translateY(-5px);
    }
  }
</style>