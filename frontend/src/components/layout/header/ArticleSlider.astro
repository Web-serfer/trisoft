---
// Демонстрационные данные для статей. В реальном проекте они могут приходить из CMS или API.
const articles = [
    { href: '/blog/new-features', title: '🔥 Новые возможности платформы уже доступны!' },
    { href: '/blog/case-study', title: 'Кейс: Как мы увеличили конверсию на 30%.' },
    { href: '/blog/upcoming-webinar', title: 'Вебинар: Тренды веб-разработки 2024.' },
];

// Дублируем массив для создания бесшовной анимации
const loopedArticles = [...articles, ...articles];
---

<div class="slider-container" aria-label="Последние статьи">
    <div class="slider-track">
        {loopedArticles.map(article => (
                <a href={article.href} class="slider-item">
                    {article.title}
                </a>
        ))}
    </div>
</div>

<style>
    .slider-container {
        /* "Маска", которая обрезает все, что выходит за ее пределы */
        overflow: hidden;
        width: 100%;
        /* Добавляем градиент по бокам для плавного появления/исчезновения текста */
        -webkit-mask-image: linear-gradient(to right, transparent, black 10%, black 90%, transparent);
        mask-image: linear-gradient(to right, transparent, black 10%, black 90%, transparent);
    }

    .slider-track {
        /* Flex-контейнер для элементов */
        display: flex;
        /* Устанавливаем ширину в 2 раза больше, так как мы дублировали контент */
        width: fit-content;
        /* Применяем нашу анимацию */
        animation: scroll 25s linear infinite;
    }

    /* При наведении на контейнер, останавливаем анимацию */
    .slider-container:hover .slider-track {
        animation-play-state: paused;
    }

    /* Анимация скроллинга */
    @keyframes scroll {
        from {
            /* Начальная позиция */
            transform: translateX(0);
        }
        to {
            /* Конечная позиция - сдвигаемся влево ровно на половину ширины трека */
            transform: translateX(-50%);
        }
    }

    .slider-item {
        /* Стили для каждой "статьи" */
        padding: 0 1.5rem; /* 24px */
        white-space: nowrap; /* Запрещаем перенос строк */
        color: inherit; /* Наследуем цвет текста от родителя */
        transition: color 0.2s ease-in-out;
    }

    .slider-item:hover {
        color: #3b82f6; /* text-blue-500 для примера */
    }
</style>
