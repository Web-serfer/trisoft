<div class="lg:col-span-2">
  <!-- Бэйджик -->
  <div class="inline-flex items-center px-4 py-2 bg-cyan-500/10 rounded-full border border-cyan-500/20 mb-8 anim-fade-in-up">
    <span class="w-2 h-2 bg-cyan-400 rounded-full mr-3 animate-pulse"></span>
    <span class="text-cyan-300 font-medium">Автоматизация бизнеса с 1С</span>
  </div>

  <!-- Заголовок -->
  <h1 class="text-4xl md:text-5xl lg:text-6xl font-bold text-white leading-tight mb-6 anim-fade-in-up animation-delay-200">
    Автоматизация процессов{' '}
    <span class="block text-transparent bg-clip-text bg-gradient-to-r from-cyan-400 via-blue-400 to-indigo-400">
      вашего бизнеса «под ключ»
    </span>
  </h1>

  <!-- Описание -->
  <p class="text-xl text-slate-300 mb-10 max-w-3xl leading-relaxed anim-fade-in-up animation-delay-400">
    Профессиональные решения на базе 1С для полной цифровизации вашего бизнеса.
    От бухгалтерского учета до управления продажами — всё в одном месте.
  </p>

  <!-- Преимущества -->
  <div class="flex flex-wrap gap-4 mb-12">
    <div class="flex items-center bg-slate-800/50 backdrop-blur-sm rounded-lg px-4 py-2 border border-slate-700/50 anim-fade-in-up animation-delay-600">
      <svg class="w-5 h-5 text-cyan-400 mr-2" fill="none" stroke="currentColor" viewBox="0 0 24 24"><path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M13 10V3L4 14h7v7l9-11h-7z"></path></svg>
      <span class="text-white font-medium">Скорость внедрения</span>
    </div>
    <div class="flex items-center bg-slate-800/50 backdrop-blur-sm rounded-lg px-4 py-2 border border-slate-700/50 anim-fade-in-up animation-delay-700">
      <svg class="w-5 h-5 text-cyan-400 mr-2" fill="none" stroke="currentColor" viewBox="0 0 24 24"><path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M9 12l2 2 4-4m5.618-4.016A11.955 11.955 0 0112 2.944a11.955 11.955 0 01-8.618 3.04A12.02 12.02 0 003 9c0 5.591 3.824 10.29 9 11.622 5.176-1.332 9-6.03 9-11.622 0-1.042-.133-2.052-.382-3.016z"></path></svg>
      <span class="text-white font-medium">Надежность 1С</span>
    </div>
    <div class="flex items-center bg-slate-800/50 backdrop-blur-sm rounded-lg px-4 py-2 border border-slate-700/50 anim-fade-in-up animation-delay-800">
      <svg class="w-5 h-5 text-cyan-400 mr-2" fill="none" stroke="currentColor" viewBox="0 0 24 24"><path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M4 7v10c0 2.21 3.582 4 8 4s8-1.79 8-4V7M4 7c0 2.21 3.582 4 8 4s8-1.79 8-4M4 7c0-2.21 3.582-4 8-4s8 1.79 8 4m0 5c0 2.21-3.582 4-8 4s-8-1.79-8-4"></path></svg>
      <span class="text-white font-medium">Поддержка 24/7</span>
    </div>
  </div>

  <!-- Кнопки -->
  <div class="flex flex-col sm:flex-row gap-4 anim-fade-in-up animation-delay-1000">
    <button class="group px-8 py-4 bg-gradient-to-r from-cyan-500 to-blue-600 text-white font-semibold rounded-xl shadow-lg hover:shadow-cyan-500/25 transform hover:-translate-y-1 transition-all duration-300 flex items-center justify-center hover:cursor-pointer">
      Заказать консультацию
    </button>
    <button class="px-8 py-4 bg-transparent border-2 border-slate-600 text-white font-semibold rounded-xl hover:border-cyan-400 hover:text-cyan-400 transition-all duration-300 hover:cursor-pointer">
      Смотреть решения
    </button>
  </div>
</div>

<style>
    @keyframes fadeInUp {
        from {
            opacity: 0;
            transform: translateY(20px);
        }
        to {
            opacity: 1;
            transform: translateY(0);
        }
    }

    .anim-fade-in-up {
        /* Изначально элемент скрыт до старта анимации */
        opacity: 0;
        animation: fadeInUp 0.6s ease-out forwards;
    }

    /* Классы для задержки анимации */
    .animation-delay-200 { animation-delay: 0.2s; }
    .animation-delay-400 { animation-delay: 0.4s; }
    .animation-delay-600 { animation-delay: 0.6s; }
    .animation-delay-700 { animation-delay: 0.7s; }
    .animation-delay-800 { animation-delay: 0.8s; }
    .animation-delay-1000 { animation-delay: 1s; }
</style>
